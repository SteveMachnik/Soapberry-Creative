{
  "name": "soapberry-creative-website",
  "version": "1.0.0",
  "description": "Soapberry Creative website",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp -R src/. public/ && echo 'Build complete'"
  }
}
